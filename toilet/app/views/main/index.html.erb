
<% @docs.each do |e|%>
<%# ('listings/listing/id')の様に範囲を絞る動作はcontrollerで行っても良い　viewは表示だけでいい%>
<%# オブジェクト.each do |変数|
  実行する処理
end
で配列の上から読み込んでいく作業の繰り返し処理を行う。配列の最後まで読み込むとendで抜け出す
%>
<%= e %>

<% end %>
